import 'package:flutter/material.dart';

class ServicesIndex extends StatelessWidget {
  const ServicesIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Servicios'),
      ),
    );
  }
}